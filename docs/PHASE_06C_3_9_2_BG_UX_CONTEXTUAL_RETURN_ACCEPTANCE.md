# PHASE 06C.3.9.2 — BG UX + contextual return acceptance

- Visible dry-run workflow labels are Bulgarian; stable internal enums and DEMO source values remain unchanged.
- Raw intake/staging status enums are mapped to Bulgarian display labels.
- The AI header uses the contextual label `Назад`, not a hard-coded home destination.
- Closing top-level AI restores the main workspace at its previous scroll position.
- Import, profile catalogue and custom CAD opened from AI return to AI when closed.
- No persistence, network, automatic mapping, rule validation, production approval or machine-ready path is added.
