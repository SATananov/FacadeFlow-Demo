import type { UnifiedSourceSession } from '../importFormatTypes'
import { detectedImportFormatLabels, importSupportStatusLabels } from '../importUiLabels'
import { formatCardFor } from '../importFormatRoutes'
interface Props { session: UnifiedSourceSession; onClear: () => void; onBack: () => void }
export function FoundationSourceInspection({ session, onClear, onBack }: Props) { const card = formatCardFor(session.selectedRoute); return <section className="foundation-inspection" aria-labelledby="foundation-inspection-title"><header><span className="route-badge">{card.badge}</span><h3 id="foundation-inspection-title">Безопасна проверка на източника</h3></header><dl><div><dt>Файл</dt><dd>{session.metadata.fileName}</dd></div><div><dt>Открит формат</dt><dd>{detectedImportFormatLabels[session.detectedFormat]}</dd></div><div><dt>MIME</dt><dd>{session.metadata.mimeType}</dd></div><div><dt>Размер</dt><dd>{(session.metadata.sizeBytes / 1024 / 1024).toFixed(2)} MB</dd></div><div><dt>SHA-256</dt><dd title={session.sha256}>{session.sha256}</dd></div><div><dt>Статус на поддръжка</dt><dd>{importSupportStatusLabels[session.supportStatus]}</dd></div><div><dt>Версия / заглавна част</dt><dd>{session.metadata.formatVersion ?? '—'}</dd></div><div><dt>Приет</dt><dd>{session.createdAt}</dd></div></dl>{session.metadata.safeSummary && <p>{session.metadata.safeSummary}</p>}{session.detectedFormat === 'DWG' && <div className="foundation-notice"><b>DWG файлът е приет за безопасен анализ. Не е преобразуван в изделие.</b><p>Необходим е реален анонимизиран DWG пример преди разработка на прочитане или преобразуване.</p></div>}{session.detectedFormat === 'DXF' && <div className="foundation-notice"><b>DXF е приет само за безопасна проверка.</b><p>Геометричните обекти не са визуализирани или преобразувани.</p></div>}{session.selectedRoute === 'TABULAR' && <div className="foundation-notice">Съпоставянето на колони и автоматичното продуктово създаване предстоят след реална спецификация.</div>}{session.selectedRoute === 'SIMULATION' && <div className="foundation-notice">Маркерът на схемата е разпознат, но възстановяване на сесия не е разрешено в тази основополагаща фаза.</div>}<div className="source-state-grid"><span>Файл приет: ДА</span><span>Визуализируем: НЕ</span><span>Анализ наличен: НЕ</span><span>Чернова генерирана: НЕ</span><span>Човешка проверка: НЕ</span><span>Машинна готовност: НЕ</span></div><div className="foundation-actions"><button type="button" onClick={onClear}>Премахни файла</button><button type="button" onClick={onBack}>Назад към избор на формат</button></div></section> }
