import type { Component3DNode } from '../threeDTypes'
interface Props { node?: Component3DNode; nominalLength?: number; onOpen?: () => void }
export function Selected3DComponentInfo({ node, nominalLength, onOpen }: Props) { return <section className="selected-3d-info" aria-live="polite"><h3>Избран 3D компонент</h3>{node ? <><b>{node.id}</b><span>{node.role} · {node.profileCode ?? 'Без профилен код'}</span><span>{Math.round(node.dimensions.x)} × {Math.round(node.dimensions.y)} × {Math.round(node.dimensions.z)} mm — концептуални размери</span>{nominalLength !== undefined && <span>Номинална демонстрационна дължина: {nominalLength} mm</span>}{onOpen && <button type="button" onClick={onOpen}>Отвори компонента в работната зона</button>}</> : <p>Изберете компонент от 3D сцената или списъка.</p>}</section> }
