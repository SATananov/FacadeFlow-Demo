import { importFormatCards } from '../importFormatRoutes'
import type { ImportRoute } from '../importFormatTypes'
interface Props { onSelect: (route: ImportRoute) => void }
export function ImportFormatChooser({ onSelect }: Props) { return <section className="import-format-chooser" data-help-id="format-selector" aria-labelledby="import-format-title"><h3 id="import-format-title">Изберете формат преди локалния файл</h3><div className="import-format-grid">{importFormatCards.map((card) => <button key={card.route} type="button" className="import-format-card" onClick={() => onSelect(card.route)}><span className="route-badge">{card.badge}</span><b>{card.title}</b><strong>{card.formats.join(' · ')}</strong><p>{card.description}</p><span className="format-capabilities">OCR: {card.route === 'IMAGE' || card.route === 'PDF' ? 'ДА' : 'НЕ'} · Анализ: {card.route === 'SKYGLAZING' ? 'само за преглед' : card.route === 'IMAGE' || card.route === 'PDF' ? 'демонстрационен' : 'НЕ'}</span>{card.status && <small>{card.status}</small>}</button>)}</div><p className="import-center-safety">Всички маршрути са локални и симулационни. Нито един формат не се изпраща към сървър или машина.</p></section> }
