import type { CameraPreset, SceneBounds3D, Vector3Value } from './threeDTypes'
export const cameraPresetLabels: Record<CameraPreset, string> = { FRONT: 'Отпред', BACK: 'Отзад', LEFT: 'Ляво', RIGHT: 'Дясно', TOP: 'Отгоре', BOTTOM: 'Отдолу', ISOMETRIC: 'Изометрия' }
export function cameraPosition(preset: CameraPreset, bounds: SceneBounds3D): Vector3Value { const distance = Math.max(bounds.width, bounds.height) * 1.45; const positions: Record<CameraPreset, Vector3Value> = { FRONT: { x: 0, y: 0, z: distance }, BACK: { x: 0, y: 0, z: -distance }, LEFT: { x: -distance, y: 0, z: 0 }, RIGHT: { x: distance, y: 0, z: 0 }, TOP: { x: 0, y: distance, z: 0 }, BOTTOM: { x: 0, y: -distance, z: 0 }, ISOMETRIC: { x: distance * .78, y: distance * .62, z: distance } }; return positions[preset] }
