import type { InspectedImportSource, UnifiedSourceSession } from './importFormatTypes'
export function createSourceSession(inspection: InspectedImportSource): UnifiedSourceSession { return { sourceId: crypto.randomUUID(), selectedRoute: inspection.selectedRoute, detectedFormat: inspection.detectedFormat, metadata: inspection, sha256: inspection.sha256, supportStatus: inspection.supportStatus, warnings: inspection.warnings, sourcePageCount: inspection.pageCount, linkedDraftIds: [], createdAt: inspection.importedAt, simulationOnly: true, machineReady: false, requiresHumanApproval: true } }
